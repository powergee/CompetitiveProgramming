#include <iostream>

char str[27];
char map[300][301];
int current[300][300];
int sum[300][300];
int dp[300][300][27];

void getSum(int bottom, int right)
{
    
}

int main()
{
    int n, m, l;
    scanf("%d %d %d", &n, &m, &l);
    scanf("%s", str);

    for (int i = 0; i < n; ++i)
        scanf("%s", map[i]);
    


    return 0;
}