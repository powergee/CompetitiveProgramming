#include <iostream>
#include <string>
#include <algorithm>

bool IsPalin(int exLeft, int exRight)
{
    
}

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int T;
    std::cin >> T;

    while (T--)
    {
        std::string input;
        std::cin >> input;


    }

    return 0;
}